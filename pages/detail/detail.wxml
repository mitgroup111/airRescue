<!--detail.wxml-->
<view class="art-header">
    <view class="art-title">{{news.title}}</view>
    <view class="art-source">{{news.hits}}</view>
</view>
<view class="art-content">
  {{news.content}}
</view> 