<view class='contentW'>
  <view class="free-WaterfallFlow">
    <block>
      <view class="flex-wrap" wx:for="{{peaple_list}}" wx:if="{{item.id%2!=''}}">
        <navigator url="productInfo?productId={{item.productId}}" hover-class="menu-navigator-hover">
          <image mode="widthFix" src="http://www.hems999.com{{item.imgPath}}"></image>
          <view class="product_name m-t-10">{{item.productName}}</view>
          <view class="product_info m-t-10">{{item.freeServiceWeixin}}</view>
        </navigator>
        <navigator class="product_btn m-t-20" url="../mobileBuy/mobileBuy?productId={{item.productId}}">立即购买</navigator>
      </view>
    </block>
    <block>
      <view class="flex-wrap" wx:for="{{peaple_list}}" wx:if="{{item.id%2==''}}">
        <navigator url="productInfo?productId={{item.productId}}" hover-class="menu-navigator-hover">
          <image mode="widthFix" src="http://www.hems999.com{{item.imgPath}}"></image>
          <view class="product_name m-t-10">{{item.productName}}</view>
          <view class="product_info  m-t-10">{{item.freeServiceWeixin}}</view>
        </navigator>
        <navigator class="product_btn m-t-20" url="../mobileBuy/mobileBuy?productId={{item.productId}}">立即购买</navigator>
      </view>
    </block>
    <block>
      <view class="flex-wrap" wx:for="{{family_lsit}}" wx:if="{{item.id%2!=''}}">
        <navigator url="productInfo?productId={{item.productId}}" hover-class="menu-navigator-hover">
          <image mode="widthFix" src="http://www.hems999.com{{item.imgPath}}"></image>
          <view class="product_name m-t-10">{{item.productName}}</view>
          <view class="product_info m-t-10">{{item.freeServiceWeixin}}</view>
        </navigator>
        <navigator class="product_btn m-t-20" url="../mobileBuy/mobileBuy?productId={{item.productId}}">立即购买</navigator>
      </view>
    </block>
    <block>
      <view class="flex-wrap" wx:for="{{family_lsit}}" wx:if="{{item.id%2==''}}">
        <navigator url="productInfo?productId={{item.productId}}" hover-class="menu-navigator-hover">
          <image mode="widthFix" src="http://www.hems999.com{{item.imgPath}}"></image>
          <view class="product_name m-t-10">{{item.productName}}</view>
          <view class="product_info  m-t-10">{{item.freeServiceWeixin}}</view>
        </navigator>
        <navigator class="product_btn m-t-20" url="../mobileBuy/mobileBuy?productId={{item.productId}}">立即购买</navigator>
      </view>
    </block>
    <block>
      <view class="flex-wrap" wx:for="{{test_list}}" wx:if="{{item.id%2!=''}}">
        <navigator url="productInfo?productId={{item.productId}}" hover-class="menu-navigator-hover">
          <image mode="widthFix" src="http://www.hems999.com{{item.imgPath}}"></image>
          <view class="product_name m-t-10">{{item.productName}}</view>
          <view class="product_info m-t-10">{{item.freeServiceWeixin}}</view>
        </navigator>
        <navigator class="product_btn m-t-20" url="../mobileBuy/mobileBuy?productId={{item.productId}}">立即购买</navigator>
      </view>
    </block>
  </view>
</view>